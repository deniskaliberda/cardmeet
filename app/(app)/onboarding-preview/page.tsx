import { OnboardingForm } from "@/components/auth/onboarding-form";

export const metadata = { title: "Onboarding Vorschau" };

export default function OnboardingPreviewPage() {
  return <OnboardingForm preview />;
}
